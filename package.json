{
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.ibm.com/GCAT/acl-logs.git"
  },
  "dependencies": {
    "axios": "^0.19.2",
    "ibm-cos-sdk": "^1.6.1",
    "qs": "^6.9.4",
    "readline-sync": "^1.4.10"
  },
  "bin": {
    "flowlog": "bin/cli.js"
  },
  "name": "acl-logs",
  "description": "When traffic is denied to a VPC by ACLs or security groups, the only way to investigate is to look at flow logs. Currently, flow logs are hard to read and manage. We are going to build a tool that runs SQL queries on the logs to generate important information in a readable format.",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "devDependencies": {}
}
